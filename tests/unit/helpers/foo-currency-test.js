
import { fooCurrency } from 'dblearn/helpers/foo-currency';
import { module, test } from 'qunit';

module('Unit | Helper | foo currency');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fooCurrency([42]);
  assert.ok(result);
});

