
import { daycountdown } from 'dblearn/helpers/daycountdown';
import { module, test } from 'qunit';

module('Unit | Helper | daycountdown');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = daycountdown([42]);
  assert.ok(result);
});

